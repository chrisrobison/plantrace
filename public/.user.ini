; PHP-FPM/CGI SAPIs read .user.ini automatically (mod_php does not — see
; the <IfModule mod_php.c> block in public/.htaccess for that case). Raises
; upload limits to match UploadStore::MAX_BYTES (src/Http/UploadStore.php).
upload_max_filesize = 64M
post_max_size = 64M
