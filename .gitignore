.DS_Store
Thumbs.db
*.log
.php-server.log

# Per-deployment config (see .env.example) — never commit real values.
.env

# Uploaded source sheets (see storage/uploads/ + docs/ARCHITECTURE.md
# "File uploads") — user content, never checked in.
/storage/uploads/*
!/storage/uploads/.gitkeep
